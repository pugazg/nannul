name: Generate Nannul terminology review Batch 005

on:
  push:
    branches:
      - main
    paths:
      - scripts/generate_terminology_review_batch_005.py
      - .github/workflows/generate-terminology-review-batch-005.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Generate Batch 005 contexts
        run: python3 scripts/generate_terminology_review_batch_005.py
      - name: Commit generated review packets
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add reviews/terminology/batch-005-*.json reviews/terminology/batch-005-*.md
          if git diff --cached --quiet; then
            echo 'Batch 005 context outputs unchanged.'
            exit 0
          fi
          git commit -m 'Generate Nannul terminology Batch 5 contexts'
          git push origin HEAD:main
