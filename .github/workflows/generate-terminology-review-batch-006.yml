# Batch 006 context generation workflow
name: Generate Nannul terminology review Batch 006

on:
  push:
    branches:
      - main
    paths:
      - scripts/generate_terminology_review_batch_006.py
      - .github/workflows/generate-terminology-review-batch-006.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Generate Batch 006 contexts
        run: python3 scripts/generate_terminology_review_batch_006.py
      - name: Commit generated review packets
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add reviews/terminology/batch-006-*.json reviews/terminology/batch-006-*.md
          if git diff --cached --quiet; then
            echo 'Batch 006 context outputs unchanged.'
            exit 0
          fi
          git commit -m 'Generate Nannul terminology Batch 6 contexts'
          git push origin HEAD:main
