name: Generate Nannul terminology review Batch 008

on:
  push:
    branches: [main]
    paths:
      - scripts/generate_terminology_review_batch_008.py
      - .github/workflows/generate-terminology-review-batch-008.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Generate Batch 008 contexts
        run: python3 scripts/generate_terminology_review_batch_008.py
      - name: Commit generated review packets
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add reviews/terminology/batch-008-*.json reviews/terminology/batch-008-*.md
          if git diff --cached --quiet; then
            echo 'Batch 008 context outputs unchanged.'
            exit 0
          fi
          git commit -m 'Generate Nannul terminology Batch 8 contexts'
          git push origin HEAD:main
